// This is a basic Flutter widget test.
//
// To perform an interaction with a widget in your test, use the WidgetTester
// utility that Flutter provides. For example, you can send tap and scroll
// gestures. You can also use WidgetTester to find child widgets in the widget
// tree, read text, and verify that the values of widget properties are correct.

// import 'package:flutter/material.dart';
// import 'package:flutter_test/flutter_test.dart';
// import 'package:get/get.dart';
// // import 'package:mail_api/app/modules/auth/login/controllers/login_controller.dart';
// import 'package:mail_api/app/service/network/getx_network_call.dart';

// import 'package:mail_api/main.dart';

void main() {
  // testWidgets('Counter increments smoke test', (WidgetTester tester) async {
  //   // Build our app and trigger a frame.
  //   await tester.pumpWidget(MyApp());

  //   // Verify that our counter starts at 0.
  //   expect(find.text('0'), findsOneWidget);
  //   expect(find.text('1'), findsNothing);

  //   // Tap the '+' icon and trigger a frame.
  //   await tester.tap(find.byIcon(Icons.add));
  //   await tester.pump();

  //   // Verify that our counter has incremented.
  //   expect(find.text('0'), findsNothing);
  //   expect(find.text('1'), findsOneWidget);
  // });

  // final apiRequist = Get.put(GetxNetworkCall());

  // test('domain_api_call', () async {
  //   // final resutl = await apiRequist.findDomain();
  //   // print(resutl);
  // });

  // test('accoutn_api_call', () async {
  //   final authController = Get.put(LoginController());
  //
  //   try {
  //     final resutl = await authController.loginUser();
  //     // print(resutl);
  //   } catch (e) {
  //     print('object $e');
  //   }
  // });
  // test('login_api_call', () async {
  //   try {
  //     // final resutl = await apiRequist.login();
  //     // print(resutl);
  //   } catch (e) {
  //     print('object $e');
  //   }
  // });
  // test('check_mail_api_call', () async {
  //   try {
  //     // final resutl = await apiRequist.checkMessage();
  //     // print(resutl);
  //   } catch (e) {
  //     print('object ${e.toString().split(':').last}');
  //   }
  // });
}
